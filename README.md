# 赛博贪吃蛇纯前端版

这是独立的纯前端版本，不依赖 Python 后端或 MySQL。游戏、分数和最高分都在浏览器本地运行，适合部署到 GitHub Pages、Vercel、Netlify，也可以直接打包成静态文件发给别人。

## 启动开发

```powershell
npm install
npm run dev
```

## 构建发布

```powershell
npm run build
```

构建产物在 `dist/`。
